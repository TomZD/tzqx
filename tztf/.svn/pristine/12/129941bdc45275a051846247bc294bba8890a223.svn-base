/**
 * 变量
 */
/**
 * 混合使用
 */
/**
 * 线性渐变
 * 只支持水平和垂直方向，滤镜在IE6、IE7中须配合width或者height属性方可生效
 * @param  {string} @startColor            起始颜色
 * @param  {string} @endColor              结束颜色
 * @param  {string} @layout 			   方向。默认:h 垂直方向
 */
/**
 * 占位符 颜色设置
 * @param  {string} @fontFamily 字体
 * @param  {string} @color      颜色
 */
/**
 * 清楚浮动产生的影响样式
 */
/**
 * 行内块化元素
 */
/**
 * 文本超出省略符号显示
 */
/**
 * 禁止用户选择文本
 */
/**
 * 解决IE6下png24半透明 图片地址需要绝对路径
 * @param  {string} @url 
 */
/**
 * 半透明
 * @param  {number} @num     透明度。取值区间0-1，默认值：0.5
 */
/**
 * 2d 缩放
 * @param  {number} @x x轴缩放比例
 * @param  {number} @y y轴缩放比例
 */
/**
 * 过渡效果
 * @param  {[type]} @property [description]
 * @param  {[type]} @duration [description]
 * @param  {[type]} @timing   [description]
 */
/**
 * 三角形
 * @param  {string} @size      大小，默认4px
 * @param  {string} @color     颜色，默认#333 
 * @param  {string} @direction 方向，默认top
 */
.m-map {
  position: relative;
}
.m-map-bd {
  height: 100%;
  background: #e8fdfe;
}
.m-map-zoom,
.m-map-legend {
  background: white;
  position: absolute;
  box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.75);
  border: 1px solid #ffffff;
  *border: 1px solid #ccc;
}
.m-map-legend {
  bottom: 10px;
  right: 10px;
  padding: 10px;
  text-align: center;
}
.m-map-legend .title {
  color: #3285ff;
  font-size: 14px;
  font-weight: bold;
  margin-bottom: 10px;
}
.m-map-legend .ct {
  position: relative;
}
.m-map-legend .line {
  border: 1px solid #d9dfeb;
  background: #e5e9f2;
  width: 2px;
  position: absolute;
  top: 11px;
  bottom: 11px;
  left: 45px;
}
.m-map-legend dl {
  font: 0/0 a;
}
.m-map-legend dt {
  height: 22px;
  cursor: pointer;
}
.m-map-legend dd {
  margin-top: 10px;
  height: 22px;
  cursor: pointer;
}
.m-map-legend b,
.m-map-legend span {
  background: #e5e9f2;
  border: 1px solid #d9dfeb;
  line-height: 20px;
  border-radius: 10px;
  color: #4e4c4f;
  font-size: 12px;
  font-weight: normal;
  display: inline-block;
  *display: inline;
  *zoom: 1;
  vertical-align: top;
}
.m-map-legend b {
  width: 30px;
}
.m-map-legend span {
  width: 40px;
}
.m-map-legend i {
  z-index: 2;
  display: inline-block;
  *display: inline;
  *zoom: 1;
  vertical-align: top;
  width: 12px;
  height: 2px;
  margin: 9px -1px 0;
  border-top: 1px solid #d9dfeb;
  border-bottom: 1px solid #d9dfeb;
  background: #e5e9f2;
  position: relative;
}
.m-map-legend dt:hover b,
.m-map-legend dd:hover b,
.m-map-legend .on b,
.m-map-legend dt:hover span,
.m-map-legend dd:hover span,
.m-map-legend .on span,
.m-map-legend dt:hover i,
.m-map-legend dd:hover i,
.m-map-legend .on i {
  background: #3286ff;
  color: white;
}
.m-map-tools {
  position: absolute;
  top: 10px;
  right: 10px;
}
.m-map-tools .btn-group {
  margin-right: 10px;
}
